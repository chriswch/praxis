{
  "name": "praxis",
  "version": "0.0.0",
  "private": true,
  "description": "A CLI that drives an AI coding agent through a deterministic, resumable workflow.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "praxis": "dist/cli.js"
  },
  "files": [
    "dist",
    "src/config/prompts"
  ],
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsdown": "^0.21.10",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  },
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.119",
    "zod": "^4.3.6"
  }
}
